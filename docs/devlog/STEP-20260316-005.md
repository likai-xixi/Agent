# STEP_ID: STEP-20260316-005

- Title: `Implement fallback policy and retry budget`
- Owner: `codex`
- Status: `done`
- Created At: `2026-03-16T14:14:56Z`

## Objective

Add fallback strategy evaluator, retry budget manager, provider failure handling, and load-test baseline

## Change Scope

1. Added fallback and retry governance modules:
   - `src/orchestrator/fallbackPolicy.js`
   - `src/orchestrator/retryBudgetManager.js`
2. Extended provider contract and adapters for deterministic failure simulation:
   - `src/providers/adapterContract.js`
   - `src/providers/openaiAdapter.js`
   - `src/providers/geminiAdapter.js`
   - `src/providers/claudeAdapter.js`
   - `src/providers/localAdapter.js`
3. Enhanced provider registry with health query and normalized provider error wrapping:
   - `src/providers/providerRegistry.js`
4. Integrated fallback strategy + retry budget enforcement into orchestrator execution and transitions:
   - `src/orchestrator/orchestratorService.js`
   - `src/orchestrator/index.js`
5. Extended audit taxonomy with provider-failure/fallback/retry events:
   - `src/platform/audit.js`
   - `src/platform/index.js`
6. Added load-test baseline and command:
   - `scripts/load-test.js`
   - `package.json` (`test:load`)
7. Updated API action behavior for `TAKEOVER` idempotency in `RUNNING` state:
   - `src/api/taskApiServer.js`
8. Added and updated tests:
   - `tests/fallback-policy.test.js`
   - `tests/retry-budget-manager.test.js`
   - `tests/orchestrator-service.test.js`
   - `tests/task-api-server.test.js`
   - `tests/contracts.test.js`
9. Updated project tracking:
   - `docs/backlog.md`
   - `docs/handoff/CURRENT.md`
   - `docs/devlog/checkpoints/CKPT-20260316-005.json`

## Commands Run

```text
node scripts/record-step.js --title "Implement fallback policy and retry budget" --objective "Add fallback strategy evaluator, retry budget manager, provider failure handling, and load-test baseline" --owner "codex" --status "in_progress" --next-step "Finalize step documentation and checkpoint metadata for STEP-20260316-005." --acceptance "Fallback policy retries across providers when retryable errors occur." --acceptance "Retry budget blocks attempts beyond configured max." --acceptance "Unit tests and load tests pass for fallback and retry flows."
npm test
npm run test:load
node scripts/verify-governance.js
git add .
git commit -m "STEP-20260316-005 implement fallback policy, retry budget, and load-test baseline"
git tag -a checkpoint/CKPT-20260316-005 HEAD -m "Checkpoint for STEP-20260316-005"
```

## Test Results

1. `npm test` passed:
   - 35 tests executed, 35 passed, 0 failed.
2. `npm run test:load` passed:
   - 80 tasks, concurrency 16, success rate 1.0, p95 latency ~21ms.
3. `node scripts/verify-governance.js` passed for all changed files including high-risk fallback/retry modules.

## Risks

1. Retry budget is currently enforced in-memory from task snapshot metadata and assumes single orchestrator writer.
2. Mitigation: keep single-writer deployment in phase 1; move retry counters to shared transactional storage for multi-instance mode.
3. Fallback policy currently uses deterministic candidate ordering and does not yet include dynamic live-cost/live-latency optimization.
4. Mitigation: adaptive model/provider routing is scheduled in subsequent steps.

## Rollback

```text
git revert --no-edit checkpoint/CKPT-20260316-005
N/A (no database schema changes in this step)
git checkout HEAD~1 -- config/feature_flags.json
```

## Next Step

Start STEP-20260316-006: implement takeover workflow hooks and IM-notifier interface with pending-human-intervention flow.
