# STEP_ID: STEP-20260316-031

- Title: `Move core persistence to structured database`
- Owner: `codex`
- Status: `done`
- Created At: `2026-03-16T23:37:39Z`

## Objective

Add SQLite persistence adapters with migration scripts and restart compatibility

## Change Scope

1. Added SQLite runtime persistence adapters:
   - file: `src/persistence/sqliteRuntimeStore.js`
   - adapters for:
     - audit event store
     - task snapshot store
     - takeover store
     - provider alert store
   - includes schema migration up/down helpers
2. Added persistence module export and runtime DB config:
   - files: `src/persistence/index.js`, `config/runtime_db.json`
3. Added migration commands and script:
   - file: `scripts/db-migrate.js`
   - `package.json` scripts: `db:migrate:up`, `db:migrate:down`
4. Integrated runtime DB path into orchestrator/API flow:
   - `src/orchestrator/orchestratorService.js` supports task snapshot + DB-backed alarm store
   - `src/api/taskApiServer.js` can bootstrap DB-backed orchestrator when `runtime_db.enabled=true`
5. Added compatibility tests for migration and restart recovery:
   - `tests/sqlite-runtime-store.test.js`
   - `tests/db-migrate.test.js`
   - `tests/task-api-server.test.js` runtime DB restart recovery case
6. Updated runbooks/docs for runtime DB rollout and rollback:
   - `README.md`, `docs/runbooks/deployment.md`, `docs/runbooks/rollback.md`

## Commands Run

```text
node scripts/record-step.js --title "Move core persistence to structured database" --objective "Add SQLite persistence adapters with migration scripts and restart compatibility" --owner "codex" --status "in_progress" --next-step "Integrate runtime DB adapters, migration scripts, and compatibility tests." --acceptance "Task, audit, takeover, and alert state can persist in structured database." --acceptance "Migration up/down scripts are available for rollout and rollback." --acceptance "Governance and regression gates pass."
node --test tests/sqlite-runtime-store.test.js tests/db-migrate.test.js tests/task-api-server.test.js tests/admin-ui.test.js
node scripts/verify-governance.js
npm test
npm run drill:readiness
npm run drill:continuity
npm run test:load
git add .
git commit -m "STEP-20260316-031 move runtime persistence to sqlite"
git tag -a checkpoint/CKPT-20260316-031 HEAD -m "Checkpoint for STEP-20260316-031"
git status --short
```

## Test Results

1. `node --test tests/sqlite-runtime-store.test.js tests/db-migrate.test.js tests/task-api-server.test.js tests/admin-ui.test.js` passed.
2. `npm test` passed:
   - 125 tests executed, 125 passed, 0 failed.
3. Required gates passed:
   - `node scripts/verify-governance.js`
   - `npm run drill:readiness`
   - `npm run drill:continuity`
   - `npm run test:load`

## Risks

1. SQLite runtime path uses `node:sqlite` experimental API and can change across Node releases.
2. Mitigation: DB persistence is gated by `config/runtime_db.json` (`enabled` defaults to `false`) and covered by compatibility tests.
3. Schema changes can affect rollback safety if down migration is skipped.
4. Mitigation: explicit up/down migration scripts and checkpoint `db_down_migration` command are documented.

## Rollback

```text
git revert --no-edit checkpoint/CKPT-20260316-031
npm run db:migrate:down
git checkout HEAD~1 -- src/persistence/sqliteRuntimeStore.js src/persistence/index.js scripts/db-migrate.js config/runtime_db.json src/orchestrator/orchestratorService.js src/api/taskApiServer.js package.json tests/sqlite-runtime-store.test.js tests/db-migrate.test.js tests/task-api-server.test.js README.md docs/runbooks/deployment.md docs/runbooks/rollback.md docs/devlog/STEP-20260316-031.md docs/devlog/checkpoints/CKPT-20260316-031.json docs/handoff/CURRENT.md
```

## Next Step

Start `STEP-20260316-032`: production readiness closeout with full regression rerun, rollback drill, and final handoff pack.
