# STEP_ID: STEP-20260316-028

- Title: `Integrate local model runtime`
- Owner: `codex`
- Status: `done`
- Created At: `2026-03-16T22:42:43Z`

## Objective

Add local inference client integration with health and capacity signals

## Change Scope

1. Replaced local adapter stub with configurable local runtime client:
   - file: `src/providers/localAdapter.js`
   - supports HTTP execution path, timeout/retry, and normalized runtime errors
   - keeps deterministic stub fallback when runtime URL is not explicitly configured
2. Added local runtime adapter tests:
   - file: `tests/local-adapter.test.js`
   - covers stub fallback, live execution parsing, runtime-unavailable normalization, and capacity signals
3. Updated operational docs:
   - `README.md` local runtime environment/configuration notes
   - `docs/runbooks/deployment.md` local runtime pre-deploy health/capacity checks

## Commands Run

```text
node scripts/record-step.js --title "Integrate local model runtime" --objective "Add local inference client integration with health and capacity signals" --owner "codex" --status "in_progress" --next-step "Implement local runtime adapter path, tests, and docs." --acceptance "Local model path executes real inference when runtime is configured." --acceptance "Local runtime health check includes capacity signals." --acceptance "Governance and regression gates pass."
node --test tests/local-adapter.test.js tests/provider-registry.test.js tests/orchestrator-service.test.js
node scripts/verify-governance.js
npm test
npm run drill:readiness
npm run drill:continuity
npm run test:load
git add .
git commit -m "STEP-20260316-028 integrate local model runtime"
git tag -a checkpoint/CKPT-20260316-028 HEAD -m "Checkpoint for STEP-20260316-028"
git status --short
```

## Test Results

1. `node --test tests/local-adapter.test.js tests/provider-registry.test.js tests/orchestrator-service.test.js` passed.
2. `npm test` passed:
   - 117 tests executed, 117 passed, 0 failed.
3. Required gates passed:
   - `node scripts/verify-governance.js`
   - `npm run drill:readiness`
   - `npm run drill:continuity`
   - `npm run test:load`

## Risks

1. Live local runtime path depends on local inference process/network socket availability.
2. Mitigation: normalized unavailable/timeout errors and retry/backoff behavior are built in.
3. Runtime may be configured with insufficient local capacity.
4. Mitigation: health check now includes explicit `capacity_signals` for rollout decisions.

## Rollback

```text
git revert --no-edit checkpoint/CKPT-20260316-028
N/A (no database schema changes in this step)
git checkout HEAD~1 -- src/providers/localAdapter.js tests/local-adapter.test.js README.md docs/runbooks/deployment.md docs/devlog/STEP-20260316-028.md docs/devlog/checkpoints/CKPT-20260316-028.json docs/handoff/CURRENT.md
```

## Next Step

Start `STEP-20260316-029`: build Web admin UI MVP with task list/detail and operator actions.
