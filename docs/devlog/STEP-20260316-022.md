# STEP_ID: STEP-20260316-022

- Title: `Add API authentication baseline`
- Owner: `codex`
- Status: `done`
- Created At: `2026-03-16T21:38:39Z`

## Objective

Implement token/JWT auth middleware with auditable identity fields

## Change Scope

1. Added API auth module and config:
   - file: `src/api/auth.js`
   - file: `config/api_auth.json`
   - supports static bearer tokens and HS256 JWT verification
2. Wired task API auth enforcement and identity propagation:
   - file: `src/api/taskApiServer.js`
   - unauthorized requests return `401` when auth is enabled
   - authorized identity is used as actor in task/discovery/takeover operations
   - request identity is attached into task metadata for auditability
3. Added auth audit event types:
   - file: `src/platform/audit.js`
   - records `API_AUTH_ACCEPTED` and `API_AUTH_REJECTED`
4. Updated API exports and docs:
   - file: `src/api/index.js`
   - file: `README.md`
   - file: `docs/runbooks/deployment.md`
   - file: `docs/backlog.md`
5. Added auth test coverage:
   - file: `tests/api-auth.test.js`
   - covers unauthorized rejection and authorized token/JWT pass path

## Commands Run

```text
node scripts/record-step.js --title "Add API authentication baseline" --objective "Implement token/JWT auth middleware with auditable identity fields" --owner "codex" --status "in_progress" --next-step "Wire auth in task API, add tests/docs, and pass governance gates." --acceptance "Unauthorized calls are rejected." --acceptance "Authorized calls pass with auditable identity fields." --acceptance "Governance and regression gates pass."
node --test tests/api-auth.test.js tests/task-api-server.test.js
node scripts/verify-governance.js
npm test
npm run drill:readiness
npm run drill:continuity
npm run test:load
git add .
git commit -m "STEP-20260316-022 add api authentication baseline"
git tag -a checkpoint/CKPT-20260316-022 HEAD -m "Checkpoint for STEP-20260316-022"
git status --short
```

## Test Results

1. `node --test tests/api-auth.test.js tests/task-api-server.test.js` passed.
2. `npm test` passed:
   - 99 tests executed, 99 passed, 0 failed.
3. Required gates passed:
   - `node scripts/verify-governance.js`
   - `npm run drill:readiness`
   - `npm run drill:continuity`
   - `npm run test:load`

## Risks

1. Misconfigured auth config may block API access unexpectedly.
2. Mitigation: auth is default OFF and can be enabled explicitly in `config/api_auth.json`.
3. Static token misuse could weaken access hygiene.
4. Mitigation: JWT path with issuer/audience checks is supported for stronger control.

## Rollback

```text
git revert --no-edit checkpoint/CKPT-20260316-022
N/A (no database schema changes in this step)
git checkout HEAD~1 -- config/api_auth.json src/api/auth.js src/api/taskApiServer.js src/api/index.js src/platform/audit.js tests/api-auth.test.js README.md docs/runbooks/deployment.md docs/backlog.md
```

## Next Step

Start `STEP-20260316-023`: implement RBAC and endpoint-level least-privilege authorization.
