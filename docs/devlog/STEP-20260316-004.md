# STEP_ID: STEP-20260316-004

- Title: `Implement task API skeleton`
- Owner: `codex`
- Status: `done`
- Created At: `2026-03-16T13:48:39Z`

## Objective

Add HTTP API skeleton for task create/get/actions and wire to orchestrator service

## Change Scope

1. Added HTTP task API server skeleton without external framework dependencies:
   - `src/api/taskApiServer.js`
   - `src/api/index.js`
2. Wired top-level exports for API module:
   - `src/index.js`
3. Implemented task endpoints:
   - `POST /tasks`
   - `GET /tasks/{id}`
   - `POST /tasks/{id}/actions`
4. Added operations endpoints:
   - `GET /audit/events?task_id=...|trace_id=...`
   - `GET /tasks/{id}/replay`
   - `GET /health`
5. Added API test coverage:
   - `tests/task-api-server.test.js`
6. Added API startup script:
   - `package.json` script `start:api`
7. Updated delivery tracking:
   - `docs/backlog.md`
   - `docs/handoff/CURRENT.md`
   - `docs/devlog/checkpoints/CKPT-20260316-004.json`

## Commands Run

```text
node scripts/record-step.js --title "Implement task API skeleton" --objective "Add HTTP API skeleton for task create/get/actions and wire to orchestrator service" --owner "codex" --status "in_progress" --next-step "Implement API handlers, tests, and checkpoint metadata finalization." --acceptance "POST /tasks creates task via orchestrator and returns task snapshot." --acceptance "GET /tasks/{id} returns current task state or 404." --acceptance "POST /tasks/{id}/actions supports APPROVE/RETRY/ABORT/TAKEOVER and records state changes."
node --test tests/task-api-server.test.js
npm test
node scripts/verify-governance.js
git add .
git commit -m "STEP-20260316-004 implement task API skeleton and audit query endpoints"
git tag -a checkpoint/CKPT-20260316-004 HEAD -m "Checkpoint for STEP-20260316-004"
```

## Test Results

1. `node --test tests/task-api-server.test.js` passed:
   - 2 tests executed, 2 passed, 0 failed.
2. `npm test` passed:
   - 27 tests executed, 27 passed, 0 failed.
3. `node scripts/verify-governance.js` passed with step/checkpoint/handoff updates.

## Risks

1. API layer currently uses in-memory task map with JSONL event persistence, which is suitable for single-instance private deployment but not horizontally scaled writes.
2. Mitigation: keep single orchestrator writer in phase 1; move to shared transactional store before multi-instance rollout.
3. `TAKEOVER` currently triggers provider execution only when `input` is provided in action payload.
4. Mitigation: keep explicit operator input requirement and add policy-driven default prompts in next workflow phase.

## Rollback

```text
git revert --no-edit checkpoint/CKPT-20260316-004
N/A (no database schema changes in this step)
git checkout HEAD~1 -- config/feature_flags.json
```

## Next Step

Start STEP-20260316-005: implement fallback strategy evaluator, retry budget control, and takeover-policy hooks.
