# STEP_ID: STEP-20260316-027

- Title: `Integrate real Claude provider adapter`
- Owner: `codex`
- Status: `done`
- Created At: `2026-03-16T22:27:50Z`

## Objective

Add Claude API execution path with timeout/retry and normalized provider errors

## Change Scope

1. Replaced Claude stub with real HTTP execution path:
   - file: `src/providers/claudeAdapter.js`
   - supports timeout/retry and normalized provider errors
   - keeps deterministic stub fallback when credentials are not explicitly configured
2. Added Claude adapter tests:
   - file: `tests/claude-adapter.test.js`
   - covers stub fallback, live-path parsing, and auth-failure normalization

## Commands Run

```text
node scripts/record-step.js --title "Integrate real Claude provider adapter" --objective "Add Claude API execution path with timeout/retry and normalized provider errors" --owner "codex" --status "in_progress" --next-step "Implement Claude HTTP client path, tests, and docs." --acceptance "Claude provider executes real requests when credentials are configured." --acceptance "Claude errors are normalized for fallback/takeover flow." --acceptance "Governance and regression gates pass."
node --test tests/claude-adapter.test.js tests/provider-registry.test.js tests/orchestrator-service.test.js
node scripts/verify-governance.js
npm test
npm run drill:readiness
npm run drill:continuity
npm run test:load
git add .
git commit -m "STEP-20260316-027 integrate claude real provider adapter"
git tag -a checkpoint/CKPT-20260316-027 HEAD -m "Checkpoint for STEP-20260316-027"
git status --short
```

## Test Results

1. `node --test tests/claude-adapter.test.js tests/provider-registry.test.js tests/orchestrator-service.test.js` passed.
2. `npm test` passed:
   - 113 tests executed, 113 passed, 0 failed.
3. Required gates passed:
   - `node scripts/verify-governance.js`
   - `npm run drill:readiness`
   - `npm run drill:continuity`
   - `npm run test:load`

## Risks

1. Live Claude path introduces external dependency latency and outage risk.
2. Mitigation: timeout/retry and normalized retryable errors are built in.
3. Credential misconfiguration can cause execution failure.
4. Mitigation: no-key safe fallback remains available and test-covered.

## Rollback

```text
git revert --no-edit checkpoint/CKPT-20260316-027
N/A (no database schema changes in this step)
git checkout HEAD~1 -- src/providers/claudeAdapter.js tests/claude-adapter.test.js docs/devlog/STEP-20260316-027.md docs/devlog/checkpoints/CKPT-20260316-027.json docs/handoff/CURRENT.md
```

## Next Step

Start `STEP-20260316-028`: integrate local model runtime with real inference client path.
