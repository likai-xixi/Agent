# STEP_ID: STEP-20260316-020

- Title: `Add alert escalation profiles and dedupe suppression window`
- Owner: `codex`
- Status: `done`
- Created At: `2026-03-16T20:35:43Z`

## Objective

Route maintenance alerts by severity profile and suppress duplicate alerts within policy windows

## Change Scope

1. Added escalation policy and suppression module:
   - file: `src/monitoring/alertEscalationPolicy.js`
   - supports severity mapping (warning/critical), profile channel routing, and dedupe windows
   - includes in-memory and JSON-file suppression stores
2. Added default policy config:
   - file: `config/alert_escalation_policy.json`
   - defines profile channels and reason-to-severity mapping
3. Wired escalation into maintenance cycle:
   - file: `scripts/audit-maintenance-cycle.js`
   - failed retention now computes escalation profile and fingerprint
   - duplicate alerts are suppressed within policy window
   - non-suppressed alerts are routed with channel + severity metadata
4. Updated notifier contract:
   - file: `src/monitoring/opsNotifier.js`
   - accepts channel and severity overrides
5. Added/updated tests:
   - new: `tests/alert-escalation-policy.test.js`
   - updated: `tests/audit-maintenance-cycle.test.js`
6. Updated docs and config references:
   - `README.md`
   - `docs/runbooks/deployment.md`
   - `docs/runbooks/rollback.md`
   - `docs/backlog.md`
   - `docs/handoff/CURRENT.md`
   - `docs/devlog/checkpoints/CKPT-20260316-020.json`

## Commands Run

```text
node scripts/record-step.js --title "Add alert escalation profiles and dedupe suppression window" --objective "Route maintenance alerts by severity profile and suppress duplicate alerts within policy windows" --owner "codex" --status "in_progress" --next-step "Implement escalation policy module, suppression store, scheduler wiring, tests, and docs." --acceptance "Maintenance failures map to warning/critical profiles with channel routing metadata." --acceptance "Duplicate alerts are suppressed within dedupe windows." --acceptance "Governance and regression suites pass with escalation policy artifacts."
npm test
npm run audit:maintenance
node scripts/verify-governance.js
npm run drill:readiness
npm run drill:continuity
npm run test:load
git add .
git commit -m "STEP-20260316-020 add alert escalation profiles and dedupe windows"
git tag -a checkpoint/CKPT-20260316-020 HEAD -m "Checkpoint for STEP-20260316-020"
```

## Test Results

1. `npm test` passed:
   - 90 tests executed, 90 passed, 0 failed.
   - includes escalation policy and suppression tests.
2. `npm run audit:maintenance` passed:
   - success path reports no escalation for healthy retention.
   - failure-path tests verify warning/critical routing and suppression behavior.
3. `node scripts/verify-governance.js` passed.
4. `npm run drill:readiness` passed:
   - governance/test/continuity/load/maintenance command checks all passed.
   - load thresholds passed (`successRate=1`, `p95LatencyMs<=150ms`).
   - API smoke checks passed.
5. `npm run drill:continuity` passed:
   - flow integrity, rollback drill, handoff drill all green.
6. `npm run test:load` passed:
   - 80 tasks, concurrency 16, success rate 1.0, p95 latency ~95.89ms.

## Risks

1. Overly aggressive dedupe windows may hide repeated important alerts.
2. Mitigation: dedupe windows are configurable per severity profile in policy config.
3. Incorrect reason mapping can under-escalate critical events.
4. Mitigation: policy file is explicit, versionable, and covered by unit tests.

## Rollback

```text
git revert --no-edit checkpoint/CKPT-20260316-020
N/A (no database schema changes in this step)
git checkout HEAD~1 -- config/alert_escalation_policy.json scripts/audit-maintenance-cycle.js src/monitoring/alertEscalationPolicy.js src/monitoring/opsNotifier.js src/monitoring/index.js package.json
```

## Next Step

Start STEP-20260316-021: integrate real IM webhook adapters (DingTalk/WeCom) for takeover and ops alerts.
