# STEP_ID: STEP-20260316-017

- Title: `Automate audit retention gate`
- Owner: `codex`
- Status: `done`
- Created At: `2026-03-16T19:31:45Z`

## Objective

Add audit archive retention verification with policy thresholds and CI scheduling hooks

## Change Scope

1. Added audit retention check script:
   - validates archive integrity by manifest/hash verification
   - enforces retention policy thresholds (`min/max archives`, `max age days`)
   - emits machine-readable report
   - file: `scripts/audit-retention-check.js`
2. Added test coverage for retention gate:
   - healthy archive set
   - tampered archive detection
   - stale archive detection
   - file: `tests/audit-retention-check.test.js`
3. Added package command:
   - `npm run audit:retention`
   - writes `docs/handoff/RETENTION-LAST.json`
   - file: `package.json`
4. Integrated retention gate into readiness and CI:
   - readiness default command chain now includes `npm run audit:retention`
   - CI workflow runs `npm run audit:retention`
   - CI uploads `RETENTION-LAST.json` as artifact
   - files:
     - `.github/workflows/governance-gate.yml`
     - `scripts/deployment-readiness.js`
5. Updated documentation:
   - `README.md`
   - `docs/runbooks/deployment.md`
   - `docs/runbooks/rollback.md`
   - `docs/backlog.md`
   - `docs/handoff/CURRENT.md`
   - `docs/devlog/checkpoints/CKPT-20260316-017.json`

## Commands Run

```text
node scripts/record-step.js --title "Automate audit retention gate" --objective "Add audit archive retention verification with policy thresholds and CI scheduling hooks" --owner "codex" --status "in_progress" --next-step "Implement retention check script, tests, and workflow integration." --acceptance "Retention check validates archive hash integrity and stale-age policy." --acceptance "Package scripts support archive + retention operations." --acceptance "CI workflow runs retention checks and publishes reports."
npm test
npm run drill:readiness
npm run drill:continuity
npm run test:load
node scripts/verify-governance.js
git add .
git commit -m "STEP-20260316-017 automate audit retention gate"
git tag -a checkpoint/CKPT-20260316-017 HEAD -m "Checkpoint for STEP-20260316-017"
```

## Test Results

1. `npm test` passed:
   - 80 tests executed, 80 passed, 0 failed.
   - includes new retention check suite.
2. `npm run drill:readiness` passed:
   - command checks passed (including `audit:retention`).
   - load thresholds passed (`successRate=1`, `p95LatencyMs<=150ms`).
   - API smoke checks passed.
3. `npm run drill:continuity` passed (`overall_passed=true`).
4. `npm run test:load` passed:
   - 80 tasks, concurrency 16, success rate 1.0, p95 latency ~86.13ms.
5. `node scripts/verify-governance.js` passed.

## Risks

1. Retention policy defaults may need environment-specific tuning (`max_age_days`, `max_archives`).
2. Mitigation: thresholds are configurable via CLI flags and persisted report for audit.
3. If archive cadence is too low, stale checks may become noisy.
4. Mitigation: align scheduler cadence with retention policy and configure alert routing.

## Rollback

```text
git revert --no-edit checkpoint/CKPT-20260316-017
N/A (no database schema changes in this step)
git checkout HEAD~1 -- .github/workflows/governance-gate.yml
```

## Next Step

Start STEP-20260316-018: add scheduled automation for archive + retention execution and alerting integration.
