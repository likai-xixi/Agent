# STEP_ID: STEP-20260316-019

- Title: `Expose audit maintenance history and query API`
- Owner: `codex`
- Status: `done`
- Created At: `2026-03-16T20:25:33Z`

## Objective

Persist audit maintenance run history and provide latest/list/failure-summary API endpoints

## Change Scope

1. Added audit maintenance history store module:
   - file: `src/monitoring/auditMaintenanceHistoryStore.js`
   - supports in-memory and JSONL persistent stores
   - stores normalized run reports with status and failure reasons
   - supports latest/list/failure summary query APIs
2. Wired maintenance scheduler to persist history:
   - file: `scripts/audit-maintenance-cycle.js`
   - each scheduler run now appends history entry
   - supports `--history-path` override
3. Exposed history APIs through orchestrator and API server:
   - file: `src/orchestrator/orchestratorService.js`
   - file: `src/api/taskApiServer.js`
   - added endpoints:
     - `GET /ops/audit-maintenance/latest`
     - `GET /ops/audit-maintenance/runs`
     - `GET /ops/audit-maintenance/failures`
4. Added and updated test coverage:
   - `tests/audit-maintenance-history-store.test.js`
   - `tests/audit-maintenance-cycle.test.js`
   - `tests/task-api-server.test.js`
5. Updated docs:
   - `README.md`
   - `docs/runbooks/deployment.md`
   - `docs/backlog.md`
   - `docs/handoff/CURRENT.md`
   - `docs/devlog/checkpoints/CKPT-20260316-019.json`

## Commands Run

```text
node scripts/record-step.js --title "Expose audit maintenance history and query API" --objective "Persist audit maintenance run history and provide latest/list/failure-summary API endpoints" --owner "codex" --status "in_progress" --next-step "Implement maintenance history store, wire scheduler persistence, add API endpoints and tests." --acceptance "Maintenance runs are persisted and queryable via API." --acceptance "Failure reasons are aggregated for operations visibility." --acceptance "Governance and regression gates pass with step artifacts."
npm test
node scripts/verify-governance.js
npm run audit:maintenance
npm run drill:readiness
npm run drill:continuity
npm run test:load
git add .
git commit -m "STEP-20260316-019 add audit maintenance history query API"
git tag -a checkpoint/CKPT-20260316-019 HEAD -m "Checkpoint for STEP-20260316-019"
```

## Test Results

1. `npm test` passed:
   - 86 tests executed, 86 passed, 0 failed.
   - includes history store and new API endpoint tests.
2. `node scripts/verify-governance.js` passed.
3. `npm run audit:maintenance` passed:
   - maintenance run succeeded and generated `history_entry`.
4. `npm run drill:readiness` passed:
   - governance/test/continuity/load/maintenance all passed.
   - load threshold passed (`successRate=1`, `p95LatencyMs<=150ms`).
   - API smoke checks passed.
5. `npm run drill:continuity` passed:
   - flow integrity, rollback drill, handoff drill all green.
6. `npm run test:load` passed:
   - 80 tasks, concurrency 16, success rate 1.0, p95 latency ~77.70ms.

## Risks

1. History JSONL file can grow with long-term operation.
2. Mitigation: store supports bounded read limits and can be rotated by ops policy.
3. Failure reason aggregation may hide per-run context if operators only read summary.
4. Mitigation: detailed run list endpoint remains available for drill-down.

## Rollback

```text
git revert --no-edit checkpoint/CKPT-20260316-019
N/A (no database schema changes in this step)
git checkout HEAD~1 -- scripts/audit-maintenance-cycle.js src/api/taskApiServer.js src/monitoring/auditMaintenanceHistoryStore.js src/monitoring/index.js src/orchestrator/orchestratorService.js
```

## Next Step

Start STEP-20260316-020: add alert escalation policy profiles with warning/critical channels and dedupe suppression windows.
