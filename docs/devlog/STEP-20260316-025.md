# STEP_ID: STEP-20260316-025

- Title: `Integrate real OpenAI provider adapter`
- Owner: `codex`
- Status: `done`
- Created At: `2026-03-16T22:08:39Z`

## Objective

Add OpenAI API execution path with timeout/retry and normalized provider errors

## Change Scope

1. Added reusable HTTP JSON client with retry/backoff:
   - file: `src/providers/httpJsonClient.js`
2. Replaced OpenAI stub with real execution path:
   - file: `src/providers/openaiAdapter.js`
   - supports API execution, timeout/retry, and normalized provider errors
   - keeps deterministic stub fallback when credentials are not explicitly configured
3. Added OpenAI adapter tests:
   - file: `tests/openai-adapter.test.js`
   - covers stub fallback, live-path parsing, and auth-failure normalization

## Commands Run

```text
node scripts/record-step.js --title "Integrate real OpenAI provider adapter" --objective "Add OpenAI API execution path with timeout/retry and normalized provider errors" --owner "codex" --status "in_progress" --next-step "Implement OpenAI HTTP client path, tests, and docs." --acceptance "OpenAI provider executes real requests when credentials are configured." --acceptance "Provider errors are normalized for fallback/takeover flow." --acceptance "Governance and regression gates pass."
node --test tests/openai-adapter.test.js tests/orchestrator-service.test.js tests/provider-registry.test.js
node scripts/verify-governance.js
npm test
npm run drill:readiness
npm run drill:continuity
npm run test:load
git add .
git commit -m "STEP-20260316-025 integrate openai real provider adapter"
git tag -a checkpoint/CKPT-20260316-025 HEAD -m "Checkpoint for STEP-20260316-025"
git status --short
```

## Test Results

1. `node --test tests/openai-adapter.test.js tests/orchestrator-service.test.js tests/provider-registry.test.js` passed.
2. `npm test` passed:
   - 107 tests executed, 107 passed, 0 failed.
3. Required gates passed:
   - `node scripts/verify-governance.js`
   - `npm run drill:readiness`
   - `npm run drill:continuity`
   - `npm run test:load`

## Risks

1. Enabling live OpenAI transport without stable network can increase timeout risk.
2. Mitigation: built-in retry/backoff and normalized timeout/rate-limit errors for fallback handling.
3. Credential misconfiguration can fail calls.
4. Mitigation: explicit no-key fallback remains available and test-covered.

## Rollback

```text
git revert --no-edit checkpoint/CKPT-20260316-025
N/A (no database schema changes in this step)
git checkout HEAD~1 -- src/providers/httpJsonClient.js src/providers/openaiAdapter.js tests/openai-adapter.test.js
```

## Next Step

Implement OpenAI HTTP client path, tests, and docs.
