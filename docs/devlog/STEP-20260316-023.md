# STEP_ID: STEP-20260316-023

- Title: `Implement RBAC least-privilege authorization`
- Owner: `codex`
- Status: `done`
- Created At: `2026-03-16T21:50:54Z`

## Objective

Add endpoint-level role enforcement for super_admin, task_admin, and read_only_auditor

## Change Scope

1. Added RBAC policy module and config:
   - file: `src/api/rbac.js`
   - file: `config/rbac_policy.json`
   - supports role checks for `super_admin`, `task_admin`, `read_only_auditor`
2. Wired endpoint-level authorization into API request handling:
   - file: `src/api/taskApiServer.js`
   - returns `403 FORBIDDEN` when role is not allowed by route policy
3. Added authz audit event types:
   - file: `src/platform/audit.js`
   - emits `API_AUTHZ_ALLOWED` and `API_AUTHZ_DENIED`
4. Updated API exports and docs:
   - file: `src/api/index.js`
   - file: `README.md`
   - file: `docs/runbooks/deployment.md`
   - file: `docs/backlog.md`
5. Added RBAC tests:
   - file: `tests/rbac.test.js`
   - validates route-level allow/deny and API integration behavior

## Commands Run

```text
node scripts/record-step.js --title "Implement RBAC least-privilege authorization" --objective "Add endpoint-level role enforcement for super_admin, task_admin, and read_only_auditor" --owner "codex" --status "in_progress" --next-step "Wire RBAC policy checks into API and validate allow/deny matrix." --acceptance "Sensitive operations require correct role." --acceptance "Audit queries are accessible by read-only auditor role." --acceptance "Governance and regression gates pass."
node --test tests/rbac.test.js tests/task-api-server.test.js tests/api-auth.test.js
node scripts/verify-governance.js
npm test
npm run drill:readiness
npm run drill:continuity
npm run test:load
git add .
git commit -m "STEP-20260316-023 implement rbac least-privilege authorization"
git tag -a checkpoint/CKPT-20260316-023 HEAD -m "Checkpoint for STEP-20260316-023"
git status --short
```

## Test Results

1. `node --test tests/rbac.test.js tests/task-api-server.test.js tests/api-auth.test.js` passed.
2. `npm test` passed:
   - 102 tests executed, 102 passed, 0 failed.
3. Required gates passed:
   - `node scripts/verify-governance.js`
   - `npm run drill:readiness`
   - `npm run drill:continuity`
   - `npm run test:load`

## Risks

1. Incorrect route-policy mapping may block legitimate operations.
2. Mitigation: RBAC defaults to OFF and has dedicated allow/deny matrix tests.
3. Role assignment mistakes can over-grant access.
4. Mitigation: role parsing is strict and only known roles are accepted.

## Rollback

```text
git revert --no-edit checkpoint/CKPT-20260316-023
N/A (no database schema changes in this step)
git checkout HEAD~1 -- config/rbac_policy.json src/api/rbac.js src/api/taskApiServer.js src/api/index.js src/platform/audit.js tests/rbac.test.js README.md docs/runbooks/deployment.md docs/backlog.md
```

## Next Step

Start `STEP-20260316-024`: harden secret management with encrypted-at-rest vault and rotation flow.
