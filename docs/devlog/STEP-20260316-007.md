# STEP_ID: STEP-20260316-007

- Title: `Implement adaptive provider routing`
- Owner: `codex`
- Status: `done`
- Created At: `2026-03-16T14:45:27Z`

## Objective

Add health/cost-aware adaptive routing and model selection for provider execution

## Change Scope

1. Added adaptive routing profiles and feature flag:
   - `config/provider_profiles.json`
   - `config/feature_flags.json` (`adaptive_routing_enabled`)
   - `src/platform/featureFlags.js`
2. Added adaptive router module:
   - `src/orchestrator/providerRouter.js`
3. Added provider health override support in registry:
   - `src/providers/providerRegistry.js`
4. Extended adapter health payloads with score/latency signals:
   - `src/providers/openaiAdapter.js`
   - `src/providers/geminiAdapter.js`
   - `src/providers/claudeAdapter.js`
   - `src/providers/localAdapter.js`
5. Integrated adaptive routing + model selection in execution path:
   - `src/orchestrator/orchestratorService.js`
   - `src/orchestrator/index.js`
6. Added API routing preview endpoint:
   - `GET /routing/preview`
   - file: `src/api/taskApiServer.js`
7. Updated docs and exports:
   - `README.md`
   - `src/index.js`
8. Added/updated tests:
   - `tests/provider-router.test.js`
   - `tests/provider-registry.test.js`
   - `tests/orchestrator-service.test.js`
   - `tests/task-api-server.test.js`
9. Updated load test to run with adaptive routing enabled:
   - `scripts/load-test.js`
10. Updated tracking:
   - `docs/backlog.md`
   - `docs/handoff/CURRENT.md`
   - `docs/devlog/checkpoints/CKPT-20260316-007.json`

## Commands Run

```text
node scripts/record-step.js --title "Implement adaptive provider routing" --objective "Add health/cost-aware adaptive routing and model selection for provider execution" --owner "codex" --status "in_progress" --next-step "Finalize STEP-20260316-007 records and checkpoint metadata." --acceptance "Adaptive routing ranks providers using health and cost signals." --acceptance "Orchestrator execution path supports adaptive routing with model selection." --acceptance "API exposes routing preview endpoint and tests/load tests pass."
npm test
npm run test:load
node scripts/verify-governance.js
git add .
git commit -m "STEP-20260316-007 implement adaptive provider routing"
git tag -a checkpoint/CKPT-20260316-007 HEAD -m "Checkpoint for STEP-20260316-007"
```

## Test Results

1. `npm test` passed:
   - 45 tests executed, 45 passed, 0 failed.
2. `npm run test:load` passed:
   - 80 tasks, concurrency 16, success rate 1.0, p95 latency ~22ms.
3. `node scripts/verify-governance.js` passed with routing-related high-risk file updates.

## Risks

1. Routing scores currently rely on static cost profiles and health hints; real-time external telemetry integration is pending.
2. Mitigation: health overrides and router interface are in place for dynamic signal ingestion in subsequent steps.
3. Model selection currently uses desired model override + default provider profile model.
4. Mitigation: add task-type model policy registry for deeper per-role optimization in future iteration.

## Rollback

```text
git revert --no-edit checkpoint/CKPT-20260316-007
N/A (no database schema changes in this step)
git checkout HEAD~1 -- config/feature_flags.json
```

## Next Step

Start STEP-20260316-008: implement provider discovery scheduler and health alarm pipeline.
